# L.U.M.U.S.-8 Exit Protocol Audit Report

## Demo artifact status

This checked-in report is a bootstrap notice. Regenerate it with:

```bash
python lumus_exit_audit.py --demo --output-dir artifacts/demo
```

The current build environment could not install Python dependencies because its PyPI proxy returned HTTP 403. The deterministic Demo mode will replace this notice with the generated audit tables and chart links when dependencies are available.

> **Survivorship bias note: the audit uses a present-day US and Japan universe; historical constituent membership is not reconstructed. Results may therefore be affected by survivorship bias.**
>
> **Demo mode results are neither empirical results nor investment-decision material.**
